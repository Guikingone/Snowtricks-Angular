export class Tricks {

    constructor(
        public name: string,
        public groups: string,
        public resume: string,
        public images?: string[],
        public videos?: string[],
    ){}
}